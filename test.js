function solution(A) {

    let sum = A.reduce((a,b)=>a+b,0);
    let sorted = A.sort((a,b) => b-a);
    let threshold = sum/2;
    let filteredSum = Infinity;
    let count = 0;
    while(filteredSum >= threshold){
        for(let i in A){
            filteredSum = 
        }
    }
    

}
solution([5,19,8,1]);